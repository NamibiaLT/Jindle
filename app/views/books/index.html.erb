<h1>Your Books:</h1>

<%= link_to 'New Book', new_book_path %>

<div id = 'books'>
  <% @books.each do |book| %>
    <h2><%= link_to book.title, book %></h2>
    <h2><%= book.author %></h2>
    <p><%= book.description %></p>
    <p><%= book.summary %></p>
  <% end %>
</div>
