<h1>Your Books:</h1>

<%= link_to 'New Book', new_book_path %>
<%= link_to 'Saved Books', book_path %>

<div id='books'>
  <% @books.each do |book| %>
    <h2><%= link_to book.name, book %></h2>
    <p><%= book.description %></p>
  <% end %>
</div>
